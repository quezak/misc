[user]
    #name =
    #email =
[color]
    ui = true
[merge]
    tool = splice
[mergetool "splice"]
    cmd = "vim -f $BASE $LOCAL $REMOTE $MERGED -c 'SpliceInit'"
    trustExitCode = true
[alias]
    s = status
    ci = commit
    co = checkout
    vd = difftool
    dt = difftool
    dts = difftool --staged
    lc = log-compact
    lcg = log-compact --graph
[core]
    pager = less
[diff]
    tool = vimdiff
    trustExitCode = true
[difftool]
    prompt = false
    trustExitCode = true
[push]
    default = simple
[log-compact]
	defaults = --decorate
